using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FreischaltscheinClass
{
    // Start is called before the first frame update
    public string Identifier;

    
    void Start()
    {
    
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
